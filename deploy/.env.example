# Copy to .env and fill in values before deploying.

# GHCR image prefix — MUST match your GitHub repo name exactly (lowercase)
# Example: repo github.com/Goldeno10/dilamme-job-scheduler → ghcr.io/goldeno10/dilamme-job-scheduler
GHCR_IMAGE_PREFIX=ghcr.io/goldeno10/dilamme-job-scheduler
IMAGE_TAG=latest

# Public domain (dynamic DNS, e.g. myscheduler.duckdns.org)
DOMAIN=scheduler.example.com
CERTBOT_EMAIL=you@example.com

# Optional
SCHEDULER_ALGORITHM=heap
DLQ_ALERT_THRESHOLD=5
DLQ_ALERT_EMAIL=ops@dilamme.com
HTTP_PORT=80
HTTPS_PORT=443

# Staging: set to 1 for Let's Encrypt staging (avoid rate limits while testing)
# CERTBOT_STAGING=1
